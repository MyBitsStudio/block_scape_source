package com.hyperledger.connector.packet;

public enum BlockchainPacketProperties {

    INVALID_PACKET, INVALID_DATA, INVALID_OPCODE,
    INVALID_LENGTH, INVALID_RADIX, INVALID_SERIAL, INVALID_SUPER,

    LAG_REPORT
}
